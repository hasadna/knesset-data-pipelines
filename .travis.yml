language: python
python:
  - 3.6
addons:
  apt:
    packages:
    - antiword
    - libreoffice
before_install:
  - '[ "${TRAVIS_TAG}" == "" ] || echo "${TRAVIS_TAG}" > VERSION.txt'
install:
  - pip install tox
script:
  - bin/test.sh
cache:
  directories:
  - "$HOME/.cache"
