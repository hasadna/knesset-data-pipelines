
location /pipelines/ {
    allow all;
    proxy_pass http://pipelines/;
}

location = / {
    allow all;
    return 200 'it works!';
    add_header Content-Type text/plain;
}

location / {
    allow all;
    root /usr/share/nginx/html/;
}
