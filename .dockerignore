.git
.idea
*.egg-info
.cache
.ipynb_checkpoints
.tox
data
tests
.dpp.db
.env
.env.example
.travis.yml
*.md
tox.ini
docker-compose*
.data-docker

# these env files are used to configure the google and kubernetes authentication and env vars
# they are used by the bin/k8s_connect.sh script
# you shouldn't source these files directly, instead, run bin/k8s_connect.sh script like this:
# source bin/k8s_connect.sh
devops/k8s/.env.*

# the Helm charts are generated using bin/templater.sh from the corresponding *.template files
# this is done in bin/k8s_recreate_templates.sh
devops/k8s/Chart.yaml
devops/k8s/charts/*/Chart.yaml

# these are custom values which are generated by some scripts or manually and allow to modify the deployed environment
devops/k8s/values-*.yaml

