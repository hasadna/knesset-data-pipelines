pipeline-type: knesset dataservice
dataservice-parameters:
  service-name: api
  method-name: "KNS_Query"
  fields:
    QueryID:
      source: "{name}"
      type: integer
      primaryKey: true
    Number:
      source: "{name}"
      type: integer
    KnessetNum:
      source: "{name}"
      type: integer
    Name:
      source: "{name}"
      type: string
    TypeID:
      source: "{name}"
      type: integer
    TypeDesc:
      source: "{name}"
      type: string
    StatusID:
      source: "{name}"
      type: integer
    PersonID:
      source: "{name}"
      type: integer
    GovMinistryID:
      source: "{name}"
      type: integer
    SubmitDate:
      source: "{name}"
      type: datetime
    ReplyMinisterDate:
      source: "{name}"
      type: datetime
    ReplyDatePlanned:
      source: "{name}"
      type: datetime
    LastUpdatedDate:
      source: "{name}"
      type: datetime
